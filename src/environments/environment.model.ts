export interface Environment {
  production: boolean;
  puzzleSize: number;
  maxErrors: number;
  firebase: {
    apiKey: string;
    authDomain: string;
    projectId: string;
    storageBucket: string;
    messagingSenderId: string;
    appId: string;
    measurementId: string;
  };
}
